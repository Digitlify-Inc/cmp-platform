# GSV Agent Store — .env example
# Copy to .env and fill with real values.
# These vars are used by:
#  - gsv-agent-store-k6-smoke.js
#  - gsv-agent-store-integration-contracts-v1.postman_collection.json (via newman globals)

API_BASE=https://api.example.com
CP_BASE=https://cp.example.com

# Optional (only used by Postman/Auth helper + some requests)
KC_BASE=https://keycloak.example.com
CONN_BASE=https://conn.example.com

# Tokens
USER_JWT=<paste-user-access-token>
SERVICE_JWT=<paste-service-jwt>
RUNTIME_JWT=<paste-runtime-service-jwt>

# Offering/Plan under test
OFFERING_VERSION_ID=ov_100
PLAN_ID=pro

# Optional pre-known IDs (otherwise CP /orgs/auto + /instances will generate during run)
ORG_ID=org_1
PROJECT_ID=proj_1
WALLET_ID=wal_1
INSTANCE_ID=inst_123
